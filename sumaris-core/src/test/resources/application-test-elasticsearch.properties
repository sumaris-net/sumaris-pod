
# Increase timeout for Unit Test
spring.elasticsearch.enabled=true
spring.elasticsearch.connection-timeout=35s
spring.elasticsearch.socket-timeout=35s
spring.elasticsearch.index.replicas=0

# DEV only
#spring.elasticsearch.uris=http://localhost:9200

# Disable Elasticsearch (dy default - will be enabled by the ElasticResource class)
spring.data.elasticsearch.repositories.enabled=false


logging.level.org.elasticsearch=warn
